// This file is obsolete. The menu data is now stored in `menu.json`.
// Keeping the file to avoid breaking imports immediately, but it should be removed in the future.
export const menuData = [];

    