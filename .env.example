
# -----------------------------------------------------------------------------
# Instructions for Environment Variables
# -----------------------------------------------------------------------------
# 1. DO NOT commit this file with your actual secrets.
# 2. For local development, rename this file to `.env.local`.
# 3. Fill in the values below with your actual Firebase project credentials.
# 4. For production (e.g., Vercel), add these variables in the project settings.
# -----------------------------------------------------------------------------

# Your full website URL (used for sitemap.xml, etc.)
# Example: https://www.yourdomain.com
SITE_URL="http://localhost:3000"

# --- Firebase Public Keys (safe to expose to the client) ---
# Get these from your Firebase project settings > General > Your apps > Firebase SDK snippet > Config
NEXT_PUBLIC_FIREBASE_API_KEY="your_api_key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your_auth_domain"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your_project_id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your_storage_bucket"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your_messaging_sender_id"
NEXT_PUBLIC_FIREBASE_APP_ID="your_app_id"

# --- Firebase Admin Keys (MUST be kept secret, for server-side use only) ---
# Get these from Firebase project settings > Service accounts > Generate new private key
FIREBASE_PROJECT_ID="your_project_id" # Same as above
FIREBASE_CLIENT_EMAIL="your_service_account_client_email"
# The private key must be enclosed in quotes.
# In Vercel, copy the entire key including "-----BEGIN PRIVATE KEY-----" and "-----END PRIVATE KEY-----".
FIREBASE_PRIVATE_KEY="your_service_account_private_key"

# --- Authentication Secret ---
# A random, secret string used for signing JWTs for sessions.
# You can generate one using an online tool or just type a random string.
JWT_SECRET="your_super_secret_jwt_key_that_is_long_and_random"
